# uClibc-ng signal tests
# Licensed under the LGPL v2.1, see the file COPYING.LIB in this tarball.

# tst-sigsimple asks features.h itself.  The suite never reads uClibc's
# .config, so this variable was always empty and the test was disabled on
# every target.
