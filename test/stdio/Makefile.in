# uClibc-ng stdio tests
# Licensed under the LGPL v2.1, see the file COPYING.LIB in this tarball.

DODIFF_64bit := 1

# The suite never reads uClibc's .config, so this variable was always empty
# and the test was disabled everywhere.  It asks features.h itself now.

TESTS_DISABLED += printf_fp
